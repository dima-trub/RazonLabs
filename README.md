Overview
This project implements an ETL (Extract, Transform, Load) pipeline to process and analyze sensor data. The pipeline reads data from CSV files, validates it using Great Expectations, transforms it, and saves the results in multiple formats. DuckDB is used for querying the transformed data.
